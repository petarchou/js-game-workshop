function initGameState() {
    const state = {
        score: 0,
        wizard: {
            scale: 1.2,
            left: 3,
            top: 40,
        },
        keys: {},
    }

    return state;
}

